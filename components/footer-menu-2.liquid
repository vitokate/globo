<div class="col-lg col-md-6 mb-lg-0 mb-4">
    <h5>{{ footer-menu-2.options.title }}</h5>
    <ul class="navbar-nav mr-auto">
        {% for item in menu.footer_2.items %}
            {% render 'footer_menu', item: item %}
        {% endfor %}
    </ul>
</div>